import React from "react";

const LEFT_BUTTON = ({handleClick}) => (
  <button className="arrowbuttonleft" onClick={handleClick}>
    <i className="arrow left"></i>
  </button>
)

export default LEFT_BUTTON;